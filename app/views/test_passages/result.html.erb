<h1><%= t('.header', title: @test_passage.test.title) %></h1>

<p>
  <% if @test_passage.test_passed? %>
    <font color="green"><%= t('.success') %></font>
  <% else %>
    <font color="red"><%= t('.failure') %></font>
  <% end %>
</p>

<%= link_to t('.back'), tests_path %>
