<% flash.each do |key, message| %>
  <% if key == 'alert' %>
    <%= content_tag :p, message, class: "alert alert-danger", role: 'alert' %>
  <% else %>
    <%= content_tag :p, message, class: "alert alert-primary", role: 'alert' %>
  <% end %>
<% end %>
