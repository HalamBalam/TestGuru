ru:
  activerecord:
    models:
      test: "Тест"

    attributes:
      test:
        title: "Название"
        category: "Категория"

    errors:
      messages:
        blank: "не может быть пустым"
      models:
        test:
          attributes:
            category:
              required: "должна быть выбрана"
